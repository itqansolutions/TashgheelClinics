{
  "name": "tashgheel-clinics",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd clinic-api && npx prisma db push --schema=src/prisma/schema.prisma --accept-data-loss && npx ts-node src/prisma/seed.ts && npm start",
    "build": "cd clinic-web && npm install && npm run build && cd ../clinic-api && npm install && npx prisma generate --schema=src/prisma/schema.prisma && npm run build"
  }
}
